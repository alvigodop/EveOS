// Unidex View Facade
// Core implementation lives in unidex-view.core.js.
(function () {
    if (!window.UnidexView) {
        console.warn('[UnidexView] Core module not loaded (unidex-view.core.js).');
    }
})();
