// --- CONTEXT MENUS FACADE ---
// Context menu runtime is split into:
// - menus.base.js: shared menu state, placement, and folder context menu stats
// - menus.surfaces.js: link/category/workspace/sidebar/unidex menu surfaces and initialization
// Keep this facade as the stable manifest anchor for legacy load order.
