// --- CATEGORIES FACADE ---
// Category runtime is split into:
// - categories.shared.js: shared helpers and scoped config movement
// - categories.move.js: card move transaction and refresh orchestration
// - categories.ui.js: card order, collapse, focus, and launch UI actions
// Keep this facade as the stable manifest anchor for legacy load order.
