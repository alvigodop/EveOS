const status=document.getElementById("status");const button=document.getElementById("connect");async function connectInstagram(){button.disabled=true;status.textContent="Reading Instagram session…";try{const cookies=await chrome.cookies.getAll({url:"https://www.instagram.com/"});if(!cookies.length)throw new Error("No Instagram cookies were found. Make sure you are signed in to Instagram in this browser.");const response=await fetch("http://127.0.0.1:8765/api/audioflix/instagram-session",{method:"POST",headers:{"Content-Type":"text/plain;charset=UTF-8"},body:JSON.stringify({cookies})});const result=await response.json();if(!response.ok||!result.ok)throw new Error(result.reason||result.message||"EveOS rejected the Instagram session.");status.textContent="✓ Connected. EveOS can now resolve Instagram videos using this session."}catch(error){status.textContent="Could not connect: "+(error?.message||String(error))}finally{button.disabled=false}}button.addEventListener("click",connectInstagram);