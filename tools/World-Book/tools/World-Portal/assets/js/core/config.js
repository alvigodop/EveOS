export const CONFIG = Object.freeze({
  textureUrl: "assets/textures/earth-blue-marble.png",
  initialCamera: [0.15, 0.18, 4.25],
  flatCamera: [0.0, 0.0, 5.55],
  flatMinDistance: 0.65,
  flatMaxDistance: 11.5,
  globeMinDistance: 2.55,
  globeMaxDistance: 7.5,
  earthRadius: 1.35,
  flatWidth: 5.2,
  flatHeight: 2.6,
  moonRadius: 0.22,
  moonOrbitRadius: 2.2,
  moonOrbitBaseSpeed: 0.32,
  spinReferenceFps: 60,
  axialTiltDegrees: -23.4,
  defaults: {
    hexDensity: 88.0,
    hexStrength: 1.0,
    hexBorderStrength: 0.58,
    hexEdgeWidth: 0.085,
    hexCohesion: 0.72,
    hexCoastSnap: 0.82,
    hexEqualArea: true,
    cartoonStrength: 0.0,
    landTintStrength: 0.0,
    saturation: 1.0,
    oceanShine: 0.35,
    spinSpeed: 0.0015,
    satellitesVisible: true,
    planetScale: 1.0,
    lightAzimuthDegrees: -52,
    lightElevationDegrees: 26,
    fullLight: false,
    projectionBlend: 0.0,
    pureFlat: false,
    latLongGridVisible: false,
    continentNamesVisible: false,
    countryStatsVisible: true,
    gridOpacity: 0.36,
    cloudOpacity: 0.24,
    cloudDriftSpeed: 0.025,
    cloudSoftness: 0.34,
    cloudsVisible: false,
    atmosphereVisible: true,
    outlineVisible: true,
    hudCollapsed: false,
    planetRadiusKm: 6371,
    collapsedSections: [],
    // Keep the immediately useful World group open and tuck the specialist
    // groups away. Both arrays are copied by the settings/world stores.
    collapsedGroups: ["group-appearance", "group-view"],
    groupsSeeded: true,
    // Every control section starts collapsed. This flips true once the panels
    // have been seeded, so a user who deliberately expands everything is not
    // re-collapsed on the next load.
    sectionsSeeded: false
  },
});
