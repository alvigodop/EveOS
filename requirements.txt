# EveOS Python runtime (tested with Python 3.10-3.11).
# Keep this file aligned with imports under server/ and server_modules/.

# Gemini Live, text-brain, and transcription paths.
google-genai==2.13.0
websockets==14.2
pydub==0.25.1
vosk==0.3.45

# Audioflix native output bridge and waveform/device utilities.
numpy==1.24.4
sounddevice==0.5.5

# Audioflix URL resolver (YouTube, SoundCloud, etc. to direct audio stream).
yt-dlp

# World Book Reader Library PDF extraction.
PyMuPDF==1.27.2.2
